# OCR 검수 시스템 시나리오

> **문서 상태**: 설계 확정 + 구현 완료. 초안 이후 실제 구현하면서 결정된
> 사항(로그인 도입, admin 계정 관리 방식 등)은 §8 변경 이력에 정리했습니다.
> 아직 확정되지 않은 항목은 §7에 남아 있습니다.

## 1. 목적

검사자는 SCT 답변(손글씨 이미지)을 AI(OCR/VLM)로 텍스트 추출하고, 그 텍스트를 종합 분석에
반영한다. 그런데 추출이 가끔 잘못되어 검사자의 종합 의견이 잘못된 방향으로 흘러가는 문제가
있다.

이 시스템의 목적은 **추출된 텍스트가 얼마나 정확한지에 대한 검수자들의 독립적인 의견을
빠짐없이 모으는 것**이다.

> **2026-08-21 변경** — 처음에는 OCR 오류의 원인을 검수자가 직접 "개선불가(필기
> 자체가 알아볼 수 없거나 텍스트가 아님)/개선가능(필기는 읽을 수 있는데 AI가
> 잘못 뽑음)"의 9종 세부분류로 골라 남기게 했다. 실제 검수자 의견 — "9개 옵션
> 드롭다운보다 OCR 난이도(1~5)를 입력하는 게 빠르다" — 을 받아들여, 검수자가
> 직접 고르는 필드를 **분류 → 난이도(§5)로 완전히 대체**했다. 개선불가/개선가능
> 구분이 필요하면(§6) 난이도 5("판독 불가")를 개선불가에 대응시켜 사후에 근사할
> 수 있지만, 세부 원인(비슷한 글자 오인식·부정 표현 오류 등)은 더 이상 검수자
> 입력으로 쌓이지 않는다 — 필요해지면 §7에서 재검토한다.
>
> **2026-08-24 — 분류 체계를 완전히 제거했다** (`13_drop_classification.sql`).
> 검수자 쪽은 이미 난이도로 대체됐고 admin 참고 코멘트도 난이도로 바뀌면서
> (§4.5), 분류 컬럼과 마스터 테이블(`ocr_classification_types`)이 어느 화면에서도
> 쓰이지 않게 됐다. 남겨두면 §6 분석에서 난이도와 분류 두 체계를 계속 함께
> 고려해야 해서 정리했다. 지우기 전 값은 `backup/` 아래에 받아뒀다.

검수 의견을 남길 때는 해당 답변의 OCR을 생성한 VLM 모델 정보(`sct_import_records.vlm_model`)도
함께 기록해둔다. 모델이 교체되거나 업데이트될 때마다 정확도가 어떻게 달라지는지 추이를
비교하는 데 쓰기 위함이다 (§6 참고).

OCR 검수에는 정해진 "정답"이 없다 — 검수자마다 판단이 다를 수 있다는 것 자체가 의미 있는
데이터다. 그래서 이 시스템은 여러 검수자의 의견 중 하나를 관리자가 "최종 채택"하는 시스템이
**아니다**. 각자의 독립적인 의견을 있는 그대로, 수정 없이 보존하는 것이 최우선이다.

이 시스템의 역할은 검수 의견을 빠르고 정확하게 입력받고, 쌓인 데이터를 다양한 조건(검수자 간
의견 일치/불일치, 난이도 값, 부정 표현 포함 여부 등)으로 검색·필터링해서 보여주는 것까지다. 쌓인 데이터 중 무엇을
실제로 AI 개선 작업에 가져다 쓸지는 이 시스템이 미리 정하지 않는다. 데이터가 충분히 쌓인 뒤
admin이나 개발자가 그 시점에 필요한 기준으로 직접 조회해서 판단한다 (§6 참고).

admin은 검수자 의견 중 하나를 "최종 결정"하는 역할이 아니라, 쌓인 의견을 열람하고 필요하면
코멘트를 남기는 역할이다 (§4.5). admin이 검수에 직접 참여할 수도 있지만, 그때는 그저
검수자 한 명일 뿐이며 의견의 무게는 다른 검수자와 완전히 같다.

## 2. 용어

| 용어 | 의미 |
|---|---|
| 검수자 (reviewer) | 이 화면에서 SCT 답변을 검토하는 사람. `ocr_reviewers` 테이블의 계정으로 로그인하며, 세션의 `reviewer_id`가 곧 신원이다 |
| 패스 | 검수자가 OCR 텍스트와 이미지가 일치한다고 판단해서, 별도 입력 없이 그대로 넘어가는 처리 |
| 타이핑 | 검수자가 확신이 서지 않아 이미지를 보고 직접 텍스트를 입력하는 처리 |
| 개선불가 | 사람이 봐도 정답을 알 수 없는 경우 (필기 판독 불가 / 텍스트 자체가 아님) |
| 개선가능 | 사람은 읽을 수 있지만 AI가 잘못 추출한 경우 |
| admin | `ocr_reviewers.role = 'admin'`인 계정. 검수자들의 의견을 열람하고, 필요하면 코멘트를 남기는 역할. **최종 결정은 내리지 않는다.** 단, 검수 인력이 적어 admin도 검수자로 참여할 수 있다 — 이때는 다른 검수자와 동등한 1표이며, admin이라고 해서 더 무게가 실리지 않는다 (§4.5의 열람 제한 참고) |

## 3. 전체 흐름 개요

```
로그인 (ocr_reviewers 계정)
   │
   ▼
목록 화면 (기본: 내가 아직 처리하지 않은 것만)
   │
   └─ 검수자가 이미지와 OCR 텍스트를 비교해서 판단
         │
         ├─ [패스] 일치한다고 판단 → 즉시 저장, 타이핑 불필요
         │
         └─ [타이핑 모드] 진입 → 검수자가 이미지를 보고 직접 텍스트 입력
               │
               ├─ 본인이 제출(패스 또는 타이핑)하기 전: 다른 검수자가 이미 처리했는지,
               │    무엇으로 처리했는지 전혀 알 수 없음 (블라인드 — 서버가 강제)
               ├─ 동일 검수자가 같은 레코드를 두 번 처리 불가
               ├─ 아직 1명만 처리한 레코드는 "두 번째 독립 처리 필요" 공유 큐에 남음
               │    (사전 할당 없음, 누구든 처리 가능)
               │
               └─ 2명 모두 처리 완료 시:
                    ├─ 검수자 화면에는 그냥 **"완료" 배지만** 표시됨(숫자·상대
                    │    내용 모두 비공개, §4.4) — 검수자 화면은 상대가 몇 명
                    │    처리했는지 몰라도 되는 독립 운영 화면이기 때문. 비교와
                    │    진행 현황 집계는 admin 화면만의 역할
                    ├─ 패스와 타이핑이 섞이거나, 난이도가 서로 달라도
                    │    합의 강제 없이 각자 그대로 저장 (오히려 의미 있는 신호)
                    └─ 2명 모두 처리된 즉시 완료(completed) 처리
                         (admin 개입 여부와 무관 — admin은 결정권자가 아님)

admin 화면 (role='admin' 계정만)
   ├─ 진행 현황 요약: 완료 / 진행중 건수, 검수자별 처리량
   ├─ 완료된 레코드 : 의견 전체 열람 + 검색/필터 (불일치, 난이도, 부정 표현, 검수자 등)
   ├─ 진행중 레코드 : 목록에 보이지만 의견 내용은 가려짐
   │                   (admin도 검수자가 될 수 있어 독립성 보호가 우선)
   └─ 필요 시 코멘트만 추가 (검수자 원본 의견은 수정하지 않음, 완료 상태에 영향 없음)
```

## 4. 화면 시나리오 상세

### 4.0 로그인 및 신원

- 계정 소스는 검수 DB의 `ocr_reviewers` 테이블이다. 별도 계정 목록을 두지 않는 이유는
  검수 의견(`ocr_review_comments.reviewer_id`)과 admin 코멘트(`ocr_admin_comments.admin_id`)가
  이미 이 테이블을 FK로 참조하고 있어서, 로그인 계정이 따로 있으면 "로그인한 사람"과
  "의견을 남긴 사람"의 매핑을 이중으로 관리해야 하기 때문이다.
- 로그인 아이디는 `username` 컬럼이다 (`email`이 아님 — 실제 운영 계정이 이메일 형식이
  아니라서 분리했다). 비밀번호는 bcrypt 해시로만 저장한다.
- **검수자 신원은 화면에서 고를 수 없다.** 제출 요청에 `reviewer_id`/`admin_id`를 담지
  않고 서버가 세션에서 채운다 — 그래야 남의 이름으로 의견을 남길 수 없다.
- 로그인하지 않으면 모든 화면은 로그인 페이지로 리다이렉트되고, 모든 API는 401을 반환한다.
- **로그인 직후 도착하는 화면은 역할에 따라 갈린다** (2026-08-24) — `role='admin'`이면
  `/admin`(열람 화면), 그 외 계정은 `/review`(검수 화면)다. admin은 검수 입력보다
  열람이 주 업무라 열람 화면이 사실상 홈이고, 그래서 화면 간 이동 링크("Admin 열람"
  탭)도 없앴다. admin이 직접 검수에 참여할 때는(§2) `/review`로 바로 가면 된다.

### 4.1 목록 화면
- **내 처리 상태 필터** (기본: "내가 아직 처리하지 않은 것"):
  미처리 / 내가 타이핑한 것 / 내가 패스한 것 / 전체.
  완료 전이라도 본인이 남긴 내용은 표시된다 (블라인드는 남의 의견만 가린다)
- 추가 필터: 검사유형(성인/청소년/아동), 기간(전일/최근 1개월/전체 기간/직접 입력,
  기본값 전체 기간), 키워드, VLM 모델, **난이도(1~5, §5.1)**, **부정 표현
  만(§5.2, 2026-08-21 추가 — 필터바 맨 앞의 체크박스. 라벨은 2026-08-24에
  "포함"으로 바뀌었다가 2026-08-31에 다시 "만"으로 되돌렸다 — 실제 필터
  동작이 처음부터 "부정 표현이 있는 것만" OR 조건이라, "포함"이라는 이름이
  실제 동작과 맞지 않았다)**
  (난이도 필터는 내가 이미 남긴 난이도로만 좁히는 필터 — 본인 의견을 좁히는
  것이라 블라인드와 무관하며, "미처리"와 동시에 걸면 정의상 0건이 된다.
  부정 표현 필터는 OCR 원문 자체를 대상으로 하므로 미처리 레코드에도 적용됨)
- **이미지가 없는 레코드는 목록에서 제외한다.** s3_key·media_id·vlm_status·ocr_text가
  모두 함께 NULL인 빈 레코드(1,353건)로, 검수할 대상이 없다
- **그리드 카드 격자 하나로만 본다** (2026-08-21 — 원래 리스트 보기 모드도
  있었으나 제거). 체크박스로 여러 건을 골라 한 번에 패스하거나, 한 건만
  체크하고 [제출]을 눌러 그 건만 패스한다(§4.2). 전수 검사에서 건당 오버헤드를
  없애는 것이 목적이다
- **카드 크기는 가독성 기준으로 정한다** (2026-08-24) — 1920×1080에서 한 줄에
  4장이 들어가도록 카드 최소 폭을 잡았다. 그전에는 5장이 들어갔는데 OCR
  텍스트·난이도 버튼이 너무 작아 읽기 어렵다는 피드백이 있었다. 카드를 키운
  만큼 안쪽 글씨(OCR 텍스트 18px, 질문 16px)와 버튼(난이도 38px)도 같이 키웠다
- 이미지는 판독 난이도를 정확히 볼 수 있도록 충분히 크게 볼 수 있어야 한다
  (카드 폭에 맞춰 축소되지만 클릭하면 원본 크기 모달이 열린다 — 판독이 어려운
  건도 이걸로 충분하다고 판단해 별도 리스트 모드는 없앴다)

> **구현 메모** — 검수 DB와 원본 `mielin` DB는 서로 다른 서버일 수 있어 SQL JOIN이
> 불가능하다. 그래서 "내가 처리한 자연 키"를 검수 DB에서 먼저 뽑아 원본 쿼리에서
> `NOT IN`으로 제외한다. 키 개수 상한(20,000)을 넘으면 응답에 경고를 실어 보낸다
> — 조용히 틀린 목록을 보여주지 않기 위함이다 (§7 성능 항목 참고).

### 4.2 패스 (일치 판단)
- **OCR 난이도(1~4, §5.1)를 먼저 고른 뒤 패스 버튼 클릭으로 즉시 저장**
  (2026-08-21 — 처음엔 난이도 없이 0클릭이었으나, "패스도 난이도 없이는
  처리되지 않아야 한다"는 판단으로 바뀌었다). 별도 저장 버튼은 없다. 판독
  불가(5)는 패스로 고를 수 없다 — 읽을 수 없는데 OCR이 맞다고 확인할 수는
  없기 때문이다(타이핑 경로를 쓴다)
- **단건 패스는 "카드 체크 + [제출]"이다** — 카드의 선택 체크박스를 켠 상태로
  [제출]을 누르면 그 건만 패스로 저장된다(입력창에 글자가 있어도 무시된다).
  별도의 [패스] 버튼은 두지 않는다: 체크 상태를 무시하는 두 번째 진입점이
  생겨 혼란스럽다는 판단이다. 체크를 켜지 않고 [제출]하면 타이핑 경로(§4.3)다
- **일괄 패스**: 카드마다 먼저 난이도를 고르고, 그 카드들을 체크한 뒤 하단
  고정 바에서 한 번에 패스 처리. 난이도를 안 고른 카드나 판독불가를 고른
  카드는 일괄 처리에서 제외되고 몇 건이 왜 빠졌는지 안내된다
- 검수자 1명이 패스했다고 레코드가 바로 완료되지 않음 — 두 번째 검수자의 독립적인 처리
  (패스 또는 타이핑)가 항상 필요
- **부정 표현(§5) 체크박스는 패스에도 적용된다** — OCR 텍스트에 죽음·자살·우울 등의
  표현이 감지되면 미리 체크된 채로 표시되고, 건드리지 않으면 그 값 그대로 패스와
  함께 저장된다. 이 체크박스는 **카드 우측 상단**에 있다(2026-08-24) — 원래는
  난이도 버튼 옆(타이핑 패널 안)에 있었는데, 이미 처리한 건은 그 패널이 접혀
  있어 값이 보이지 않았다. 카드 헤더로 올려 접힘 여부와 무관하게 항상 보이고
  바로 켜고 끌 수 있게 했다

> **구현 메모** — 제출 응답에 갱신된 상태가 들어 있어 목록을 다시 불러오지 않는다.
> 처리 즉시 포커스가 다음 미처리 카드의 체크박스로 이동한다.

> **일괄 패스의 전제와 방어 장치** — 일괄 패스는 검수량(전수 44,000여 건)을 감당하기
> 위한 것이지만, "이미지를 실제로 보고 판단했다"는 전제가 깨지면 검수 데이터 자체가
> 무의미해진다. 그래서:
>
> - **"전체 선택" 버튼을 의도적으로 두지 않는다.** 한 건씩 체크해야 한다
> - 1회 상한 50건 (서버가 강제, 초과 시 422)
> - 페이지/필터가 바뀌면 선택은 초기화된다 — 화면에 보이지 않는 건을 패스하지 않도록
>
> **카드 체크박스는 목록을 불러올 때 기본으로 켜져 있다** (2026-08-31 변경) —
> 대부분 OCR이 그대로 맞아서 매번 체크하는 반복 클릭이 불필요했기 때문이다.
> 다만 "전체 선택 버튼"과는 다르다: 난이도는 여전히 카드마다 직접 골라야만
> 제출되므로(§5.1, 아래 난이도 규칙 그대로), 체크가 미리 되어 있어도 안 맞는
> 건까지 아무 입력 없이 한꺼번에 넘어가지는 않는다. 안 맞는 건은 타이핑을
> 시작하면 그 카드의 체크가 자동으로 풀린다.
>
> 일괄 패스는 **패스만** 대상이다. 타이핑은 건별로 내용과 난이도가 달라 묶을 수 없다.
> 패스도 이제 난이도(1~4)가 필수라 건별로 값이 다르지만, 값 자체가 서버 검증을
> 통과하는 범위(1~4) 안에서 자유로우므로 한 요청에 묶어도 문제가 없다.
> 한 건이 중복(이미 처리함)이어도 나머지는 그대로 저장된다 — all-or-nothing으로 묶으면
> 다른 검수자가 끼어든 순간 아무것도 저장되지 않는다.
>
> 일괄 패스도 블라인드를 깨지 않는다: 응답에 담기는 상태는 §4.3의 규칙을 그대로 따른다.

### 4.3 타이핑 (확신 안 섬)
- 행을 벗어나지 않고 인라인으로 확장 (별도 페이지 이동 없음). 입력창은 항상
  펼쳐져 있어 따로 열 필요가 없다(2026-08-20 변경 — 이전엔 [타이핑] 버튼을
  눌러야 열렸음)
- OCR 텍스트는 계속 보이는 상태로 유지 (숨기지 않기로 결정)
- 검수자가 자신이 읽은 텍스트를 직접 입력
- **[⤵ OCR 복사] 버튼(단축키 `Ctrl+Enter`)으로 OCR 텍스트를 가져와 다른
  부분만 고칠 수 있다** (2026-08-21 추가, §5.3) — 전체를 다시 칠 필요가 없다
- **입력하는 동안 OCR과 다른 부분이 실시간으로 표시된다** (§5.3). 제출하면
  그 차이가 자동으로 기록되어 나중에 필터링·분석에 쓸 수 있다
- **OCR 난이도(1~5, §5.1)는 패스/타이핑 구분 없이 항상 필요하다** (2026-08-21 —
  분류를 대체하며 규칙이 바뀜. 처음엔 타이핑에서만 필수였다가, "패스도 난이도
  없이는 처리되지 않아야 한다"는 판단으로 패스까지 넓어졌다). 텍스트는 선택
  사항이다 — "판독 불가"(레벨 5)처럼 옮겨 적을 내용 자체가 없는 경우를 위함.
  난이도를 고르지 않으면 제출을 막는다. 판독 불가(5)는 패스와 동시에 성립할
  수 없어(읽을 수 없는데 OCR이 맞다고 확인할 수는 없다) 패스로는 제출할 수
  없다 — 이때는 타이핑 경로를 쓴다
- **부정 표현(§5.2) 체크박스** — OCR 텍스트에서 자동 감지되면 미리 체크된 채로
  열리고, 검수자가 직접 켜고 끌 수 있다. 위치는 카드 우측 상단이다(§4.2 참고)
- **검수자 코멘트 입력칸은 없다** (2026-08-21 제거, §5.4)
- 다른 검수자가 이미 패스했는지 타이핑했는지, 그 내용이 무엇인지는 본인이 제출하기
  전까지 전혀 알 수 없음 (독립적 판단 보장이 목적)
- 제출 시 (assessment_id, drawing_id, answer_index, reviewer_id) 조합으로 유일성 보장
  → 동일 검수자의 중복 처리 방지

- **본인이 남긴 의견은 패스든 타이핑이든 나중에 수정할 수 있다** (2026-08-21 —
  처음엔 타이핑만 가능했으나 패스까지 넓어졌다). 단 남의 의견은 절대 수정할
  수 없고, 수정 직전 값은 이력으로 보존된다 (아래 참고)

> **수정과 §1의 관계** — §1은 "각자의 독립적인 의견을 있는 그대로, **수정 없이**
> 보존하는 것이 최우선"이라고 못박고 있다. 그래서 수정 기능은 다음 조건에서만
> 원칙과 양립한다:
>
> - **본인 것만** 수정 가능 (남의 의견 수정 시도는 403)
> - 수정 직전 값은 `ocr_review_edits`에 append되어 **원래 판단이 영구 보존**된다
> - 레코드가 이미 완료(2/2)된 뒤의 수정은 `was_completed = 1`로 기록된다. 그
>   값은 "독립적인 판단"으로 보기 어려우므로 §6에서 데이터를 가져다 쓸 때
>   따로 취급해야 한다. **상대 검수자의 내용을 실제로 봤다는 뜻은 아니다** —
>   완료 후에도 상대 내용은 절대 공개되지 않는다(§4.4). 다만 "이미 완료됐다"는
>   사실 자체는 배지로 보이므로, 그 사실만으로도 판단이 흔들릴 가능성은
>   남는다는 뜻으로 이 값을 쓴다.
> - **review_type은 저장된 값이 아니라 typed_text/난이도로 매번 다시
>   계산된다** (2026-08-21, §5.1) — "패스였다/타이핑이었다"는 고정된 정체성이
>   아니라 지금 뭘 제출했는가로 결정된다:
>   - 텍스트를 채워 저장하면 → 타이핑으로 분류 (패스 표시가 사라짐)
>   - 텍스트를 비우고 난이도 1~4를 남기면 → 패스로 분류
>   - 텍스트를 비우고 난이도 5(판독 불가)를 고르면 → 타이핑(판독 불가)으로 분류
>   즉 패스 ↔ 타이핑 양방향으로 자유롭게 전환된다. 부정 표현 체크박스는 전환
>   여부와 무관하게 마지막으로 남긴 값이 그대로 유지된다.
> - **이전 입력 내용은 저장하지 않는다** (2026-08-24 변경, §8). 최종 검수 결과가
>   목표이고 입력 중의 오타 수정까지 남길 이유가 없다는 판단이다. 대신 화면에
>   `수정 2회`처럼 **횟수**를 보여주고, 완료 후 수정이 있었으면 함께 표시한다 —
>   여러 번 고쳤다는 사실은 "검수 기준이 안 잡혔다"는 관리 신호이고,
>   `was_completed`는 블라인드가 지켜졌는지 확인하는 근거이기 때문이다.
>   **상대 검수자의 수정 정보는 볼 수 없다** — 완료 후에도 상대 내용 자체가
>   공개되지 않으므로(§4.4), 보이는 것은 항상 본인 것뿐이다.

> **구현 메모 (블라인드는 서버가 강제한다)** — 화면에서만 가리면 개발자도구로 그대로
> 들여다볼 수 있다. 그래서 내가 제출하기 전에는 API 응답 자체에 다른 검수자의 내용은
> 물론 **처리 여부(`review_count`/`status`)조차 담기지 않는다.** §3의 "이미 처리했는지
> 전혀 알 수 없음"을 지키려면 개수까지 숨겨야 하기 때문이다.
>
> 중복 처리(유니크 제약 위반)는 409로, 존재하지 않는 검수자(FK 위반)는 400으로
> 구분해서 응답한다. 난이도(1~5) 범위를 벗어난 값은 요청 형식 오류(422)다.

### 4.4 비교 및 난이도
- 검수자 2명의 처리(패스 또는 타이핑)가 모두 모이면 완료 처리됨. **완료된
  뒤에도 상대 검수자의 처리 내용은 절대 공개하지 않는다** (2026-08-21 변경) —
  "검수자 간에 서로 내용을 확인할 필요가 없다"는 판단에 따라, 화면뿐 아니라
  **API 응답 자체에서도** 상대방 내용을 뺐다. 두 결과를 나란히 비교하는 건
  admin 화면(§4.5)만의 역할이다 — admin 화면은 이 블라인드 로직(`_blind_state`)을
  쓰지 않는 완전히 별도의 엔드포인트라 이번 변경과 무관하다
  - (이전에는 완료 시 API 응답에 양쪽 내용이 다 담겨 내려갔고, 화면(review.js)이
    그중 본인 것만 그려서 안 보여줬을 뿐이었다 — 개발자도구로 보면 상대 내용이
    그대로 새어나가는 구조였다. 지금은 서버가 응답 자체에서 뺀다)
- **검수자 화면의 "완료" 배지에는 숫자(n/2)가 없다** (2026-08-21 추가 변경) —
  "완료 (1/2)"처럼 아직 상대가 안 왔는데 "완료"라고 읽히는 게 혼란스러웠고,
  본질적으로 검수자 화면은 **상대가 몇 명 처리했는지 알 필요가 없는 독립
  운영 화면**이기 때문이다. 그래서 `review_count`/`status` 자체를 API
  응답에서도 뺐다 — 이제 이 배지는 오직 "나는 이미 처리했다"만 뜻한다.
  진행 현황(몇 명이 처리했는지)을 세는 건 admin 화면(§4.5)의 역할로 남는다
- 패스한 검수자는 별도 난이도 입력 없이 "일치"로 그대로 기록됨 (부정 표현 체크박스는
  패스에도 적용된다, §4.2)
- 타이핑한 검수자는 OCR 난이도(1~5)를 항상 선택한다 — "OCR과 다를 때만"이라는
  조건이 없다(§5.1 — 필기 판독 난이도 자체를 묻는 값이라 타이핑 결과가 우연히
  OCR과 같아도 유효한 정보다). OCR과 다른 부분은 §5.3처럼 자동 기록된다
- 두 검수자의 처리 결과(패스/타이핑, 난이도)가 서로 달라도 그대로 각자 저장
  (강제 합의 없음)
- 2명 모두 처리되면 그 즉시 완료(completed) 처리됨 (admin 개입 여부와 무관)

> **구현 메모 (난이도 선택 시점)** — 난이도는 **타이핑 제출과 동시에** 받는다.
> "내가 얼마나 어렵게 느꼈는가"라는 본인만의 판단이라 상대방 결과가 필요 없고,
> 제출 시점에 받는 편이 블라인드 원칙에도 더 안전하기 때문이다.

### 4.5 Admin 열람 및 코멘트
- **`role = 'admin'`인 계정만 접근 가능** — URL로 직접 접근해도 403을 받는다.
  admin 계정은 로그인하면 이 화면으로 바로 들어온다(§4.0)
- admin은 최종 결정권자가 아니라, 검수자들의 의견을 확인하고 필요하면 코멘트를
  남기는 역할
- **진행 현황 요약**: 전체 SCT 검사 수(assessment_id 기준) / 전체 검사자 수(client_id
  기준, 같은 사람이 여러 번 검사받을 수 있어 검사 수와 다름) / 완료 건수 / 진행중
  건수. 건수 집계일 뿐 의견 내용을 노출하지 않으므로 블라인드와 무관하다.
  **패스도 "처리한 것"으로 센다** (2026-08-24 변경, 위 2026-08-20 규칙을
  뒤집음) — 검수자가 이미지를 보고 "OCR이 맞다"고 판정한 것도 분명히 처리한
  것이라, 이제 완료 + 진행중 = 검수 기록이 있는 전체 레코드다(목록 전체
  건수와 정확히 맞는다). **진행중 카드는 검수자별로 나눠서 보여준다**
  (2026-08-31 변경) — "1명만 처리" 총합 하나보다, 어느 검수자가 밀리고
  있는지 바로 보이는 게 더 쓸모 있다는 판단이다. 현역 검수자 수만큼 카드가
  생긴다(지금은 검수자 A/B 2장)
- **완료된 레코드**: OCR/검수자 A/검수자 B 내용을 나란히 확인 — 검수자 화면에서는
  더 이상 안 보여주는(§4.4 참고) 비교를 admin 화면이 전담한다
- **진행중 레코드**: 목록에 나타나며, **의견이 하나라도 등록됐으면 내용을 공개한다** (아래 참고)
- **완료된 레코드의 타이핑 줄은 §5.3의 대괄호 표기로 보여준다** — OCR 원문이
  바로 위에 있으므로 어디가 달랐는지 눈으로 바로 비교된다
- **"OCR / 검수자 입력" 칸 맨 위에 SCT 질문을 함께 보여준다** (2026-08-26) —
  검수자 카드(§4.1)와 같은 위치·같은 이유다. "내가 늘 원하기는" 같은 문두를 알면
  뒤에 이어질 필기가 무엇인지 짐작되므로, 두 검수자의 판독이 갈린 이유를 볼 때
  필요하다. 판독 대상(OCR 원문·검수자 입력)과 섞이지 않게 3줄까지 보여주고
  전체 문장은 마우스를 올리면 나온다. 글자색은 진한 파란색이다(2026-08-31,
  가독성 개선 요청 — 원래는 회색이었는데 바로 아래 OCR 원문과 크기가 같아
  구분이 잘 안 됐다).
  **문항 번호는 별도 칸 없이 질문 앞에 `[번호]`로 붙인다** (2026-08-31 변경,
  예: `[36] 시간을 되돌릴 수 있다면 _________________`) — 원래 있던 "문항"
  칼럼을 없애고 그 정보를 질문 자리로 옮겼다
- 검색/필터 기능으로 원하는 리스트를 자유롭게 뽑아볼 수 있어야 함. 화면에 있는 것:
  - **검수 상태 드롭다운** (2026-08-24 — 기본값 "타이핑한 것"). 서로 배타적인
    상태 하나만 고른다:
    - *타이핑한 것* **(기본)** — 한 명이라도 **텍스트를 실제로 옮겨 적은** 레코드.
      판독 불가는 여기 포함되지 않는다(아래 참고)
    - *OCR 텍스트와 다른 것* — 한 명이라도 OCR과 실제로 다르게 입력한 것(§5.3)
    - *판독 불가* — 한 명이라도 판독 불가로 판정한 레코드
    - *패스한 것* — **타이핑도 판독 불가도 한 명 없는** 레코드. 두 명 다
      패스했거나, 한 명만 패스하고 나머지는 아직 미처리인 경우까지 포함한다
    - *전체 (처리 여부 무관)* — 필터 없음

    > **판독 불가를 타이핑에서 분리한 이유** (2026-08-24) — DB에는 둘 다
    > `review_type='transcription'`으로 저장되지만, "읽을 수 없어 판정을 못
    > 했다"와 "OCR이 틀려서 다시 적었다"는 §6 분석에서 정반대 의미다. 예전에는
    > 판독 불가가 "타이핑한 것"에 섞여 나와 구별할 수 없었다.
    >
    > 판정 기준은 난이도 5가 아니라 **"transcription인데 옮겨 적은 텍스트가
    > 없다"**다. 텍스트 없이 난이도 1~4를 낸 제출은 서버가 패스로 재분류하므로
    > (§4.3), transcription으로 남은 채 텍스트가 없으면 난이도 5이거나 난이도
    > 도입 이전 레거시(NULL)뿐이라는 구조적 불변식이 성립한다. 난이도 5만 보면
    > 레거시 건이 세 필터 어디에도 안 걸려 "전체"에서만 보이는 사각지대가
    > 생긴다 — 실제로 옛 분류로 남긴 5건이 그랬다.
    >
    > 이 정의 덕분에 **타이핑한 것 / 판독 불가 / 패스한 것이 레코드 전체를
    > 빠짐없이 나눈다**(세 필터 건수 합 = 전체 건수). 단, 검수자가 서로 다르게
    > 판정한 레코드(A=타이핑, B=판독 불가)는 "한 명이라도" 기준이라 양쪽에 모두
    > 나온다 — 의견이 갈렸다는 사실 자체가 정보이므로 의도한 동작이다.
    >
    > **주의** — 상단 진행 현황 요약은 아직 `review_type='transcription'`으로
    > 세므로 판독 불가를 "타이핑"에 포함한다. 목록 필터와 기준이 다르다
    > (패스만 있는 레코드를 요약에서 빼는 것과 같은 종류의 불일치, §7).
  - **부정 표현만** 체크박스 — 위 드롭다운과 **독립적으로** 조합된다
    (2026-08-24, 라벨은 2026-08-31에 "포함"에서 "만"으로 정정). 부정 표현은
    타이핑/패스 어느 쪽과도 같이 걸릴 수 있는 별개 속성이라 배타적 드롭다운에
    끼워 넣으면 "타이핑 + 부정 표현" 같은 조회가 막히기 때문이다. 검수자
    화면(§4.1)의 같은 체크박스와 구조를 맞췄다
  - 특정 난이도(1~5)만
  - 특정 연령대만, 특정 VLM 모델만 (SCT 원본 메타데이터 — 블라인드와 무관하므로
    진행중 레코드에도 그대로 적용됨)
  - 특정 검수자가 참여한 것만. **검수자를 특정하면 위 드롭다운의 의미가 바뀐다** —
    "누구든 한 명이라도"가 "그 검수자 본인이"로 좁혀진다 (예: A=패스/B=타이핑인
    레코드는 "타이핑한 것"에서 전체·B 선택 시 보이지만 A 선택 시엔 안 보인다)
  - **완료 여부** — 전체 / 완료(2명) / 진행중(1명). "검수자" 필터와는 서로
    다른 질문("누가"와 "몇 명")이라 별도 컨트롤로 두고 AND로 조합했다
    (2026-08-31 신설 — 서버 파라미터(`status`)는 원래 있었지만 화면 컨트롤이
    없었다. 아래 표 참고)
  - 조회 기간 (전일/최근 1개월/전체 기간/직접 입력, §4.1과 동일한 방식)
  - 의견 불일치만 필터는 **서버 계산까지 제거됐다** (2026-08-27, §4.5 구현 메모
    참고 — 기준을 다시 정하는 일은 §7에 남아 있다)
  - 그 외 필요에 따라 조합 가능한 검색/필터
  - **필터 조합 결과가 0건이면, 지금 켜진 필터 목록과 [필터 초기화] 버튼을
    함께 보여준다** (2026-08-31, 검수자 화면(§4.1)에 있던 것을 admin에도
    적용 — 컨트롤이 8개 가까이 되면서 "필터 탓에 0건인지 고장인지" 구분이
    안 된다는 지적을 반영했다)
- **아직 아무도 처리하지 않은 레코드는 이 화면에 나오지 않는다** — "전체"를
  골라도 마찬가지다. 목록의 출발점이 `v_sct_review_status`(= 의견이 1건 이상
  있는 레코드)이기 때문이고, 이는 의도된 동작이다: 미처리 건을 찾아 처리하는
  것은 검수자 화면(§4.1)의 역할이고 admin 화면은 "남겨진 의견을 확인하는" 곳이다
- admin의 코멘트는 참고용으로 별도 보존되며, 검수자 원본 의견을 수정하거나
  완료 상태에 영향을 주지 않음. **코멘트에 난이도(1~5)도 함께 남길 수 있다**
  (2026-08-24, `ocr_admin_comments.difficulty_level`) — 두 검수자가 다른 난이도를
  냈을 때의 **중재값**이다. 검수자 난이도와 별개 컬럼이며, 관리자는 두 의견을
  모두 본 뒤 판단하는 **비블라인드 판정**이라 §1의 독립 판단이 전제인 값과
  섞지 않는다. 완료 판정에는 영향을 주지 않음
- **admin 코멘트는 검수자 레코드당 1건이며 수정 가능** (2026-08-19 추가,
  `uq_admin_comment_per_admin`) — 검수자가 본인 타이핑을 §4.3처럼 고치는 것과
  같은 방식이다. 화면 스타일도 검수자의 "내가 남긴 처리 내용" 카드와 동일하게
  맞췄고, "검수자 의견" 칸에도 `(코멘트)` 표시를 붙여 같이 노출한다 — 원본
  검수 의견과 섞여 보이지 않도록 구분만 해둔다

> **공개 조건 (2026-08-19 변경)** — 의견이 하나라도 등록되면 내용을 공개한다
> (완료 여부 무관). admin도 검수자로 참여할 수 있어(§2) 두 번째 의견을 기다리지
> 않고 먼저 읽으면 이론상 admin 본인 판단의 독립성이 깨질 수 있지만, 소수 인원
> 으로 전수 검사를 진행하는 현재 운영 방식상 admin이 진행 상황을 빠르게 파악하는
> 실익이 더 크다고 보고 이렇게 정했다. 난이도·검수자 필터는 항상 그대로 적용된다
> (더 이상 가려진 레코드가 없으므로 필터가 정보를 흘릴 통로도 없다).
>
> 이전에는 **레코드가 완료됐거나 admin 본인이 이미 의견을 남긴 경우**에만 공개해
> 독립성을 우선했었다 — 운영 편의를 위해 완화한 것이니, 재검토가 필요하면 §7 참고.

> **구현 메모 (불일치 판정, 2026-08-27 제거)** — 서버가 완료 레코드의 불일치
> 여부를 계산해 `mismatch` 필드로 내려보내고 `?mismatch=` 필터도 받았는데,
> **계산·필터·응답 필드를 모두 지웠다.**
>
> 기준이 **난이도값 비교 + 패스는 전부 일치**였고, 그러면 "둘 다 패스인데
> 난이도가 2 vs 1"인 건을 놓쳐 실제 불일치 3건 중 1건만 잡혔다. 화면 노출은
> 2026-08-19부터 보류된 상태였으니 **아무 화면도 쓰지 않는 틀린 계산이 코드로
> 남아 있던 셈**이고, 그대로 두면 중재 대기열을 만들 때 이 기준을 물려받게 된다.
> 완료된 레코드는 계속 "완료 (2/2)"로만 표시한다. **기준을 다시 정하는 일은
> §7에 그대로 남아 있다** — 그때는 코드가 아니라 기준부터 시작하게 된다.

## 5. OCR 난이도 및 부정 표현 체계 (2026-08-21)

> 실제 검수자 의견 2건을 받아들여 이 절을 다음처럼 바꿨다: (1) 9종 분류
> 드롭다운을 없애고 OCR 난이도(1~5) 단일 입력으로 대체, (2) 부정 표현(죽음·
> 자살·우울 등) 자동 감지 체크박스 신설. §1/§4.3/§4.4/§4.5에도 이 변경이
> 반영되어 있다.

### 5.1 OCR 난이도 (분류를 대체)

타이핑 처리에서 검수자가 항상 선택하는 필드다 (`ocr_review_comments.
ocr_difficulty_level`, `08_add_review_difficulty_level.sql`). "OCR과 다를
때만" 같은 조건이 없다 — 필기 판독 난이도 자체를 묻는 값이라, 타이핑 결과가
우연히 OCR과 같아도 유효한 정보이기 때문이다.

| 레벨 | 라벨 |
|---|---|
| 1 | 매우 쉬움 (정자체 및 완벽한 인식 수준) |
| 2 | 쉬움 (일반적인 필기체) |
| 3 | 보통 (주의 및 문맥 파악 필요) |
| 4 | 어려움 (심한 악필 및 복잡한 구조) |
| 5 | 판독 불가 |

> **버튼 UI (2026-08-21 변경)** — 화면에는 1~4와 5(판독 불가)를 하나의
> 숫자 버튼 5개로 늘어놓지 않는다. "읽었는데 얼마나 어려웠나"(1~4, 그레이드)와
> "읽는 것 자체가 불가능했다"(판독 불가)는 서로 다른 질문이라, 1~4 버튼과
> **별도의 [판독 불가] 버튼**으로 나눠 두고 둘을 하나의 선택 그룹으로 묶어
> 상호 배타적으로 만들었다(판독 불가를 고르면 1~4는 선택 해제됨, 반대도
> 마찬가지). 저장되는 값은 그대로 `ocr_difficulty_level=5`라 스키마·API는
> 바뀌지 않는다 — 화면의 선택 방식만 바뀐 것이다.

옛 "개선불가/개선가능" 그룹 구분(§1 참고)이 필요하면, 레벨 5("판독 불가")를
개선불가에 근사시켜 분석 단계에서 판단할 수 있다 — 다만 세부 원인(비슷한
글자 오인식, 부정 표현·의미 반전 오류 등)은 더 이상 검수자 입력으로 쌓이지
않는다. 이 근사가 부족하면 §7에서 재검토한다.

> **분류 체계는 완전히 제거됐다** (2026-08-24, `13_drop_classification.sql`).
> 검수자 의견·admin 코멘트의 `classification` 컬럼과 마스터 테이블
> `ocr_classification_types`를 모두 삭제했다 — admin 코멘트까지 난이도로
> 바뀌면서 어느 화면에서도 쓰이지 않게 됐기 때문이다. 지우기 전 값은
> `backup/` 아래에 있다.

### 5.2 부정 표현 자동 감지

패스/타이핑 공통으로 적용되는 체크박스다 (`ocr_review_comments.
contains_negative_expression`, `09_add_review_negative_expression.sql`).
SCT 분석에서 죽음·자살·우울 등의 표현은 그 자체로 중요한 신호라, 검수자가
놓치지 않도록 자동으로 미리 표시해준다.

- OCR 텍스트에 `ocr_negative_keywords` 마스터 테이블의 활성 키워드가
  하나라도 포함되면 서버가 목록 조회 시점에 미리 계산해 `auto_negative_flag`로
  내려주고, 화면은 체크박스를 그 값으로 미리 켜서 보여준다
- 검수자가 직접 켜고 끌 수 있다 — 자동 감지가 놓친 간접적인 표현을 검수자가
  판단해서 추가로 체크하거나, 오탐이면 해제할 수 있다
- 키워드는 코드 배포 없이 테이블에 INSERT/비활성화만으로 추가·조정 가능하다
  (마스터 테이블로 값을 관리하는 방식)
- 두 검수자의 체크 여부가 서로 달라도(같은 OCR 텍스트를 보고도) 강제 합의
  없이 각자 그대로 저장된다 — 이 시스템의 기본 철학(§1)과 동일하다

### 5.3 OCR과 다른 부분 자동 기록 (2026-08-21)

실제 검수자 의견 2건을 반영한 것이다: "텍스트를 최대한 빨리 입력할 방법이
필요하다", "OCR과 다른 부분을 표시하고 그 부분을 자동으로 기록해서 나중에
필터링·분석에 쓰고 싶다".

**입력 속도 — [OCR 복사]**

타이핑 패널에 [⤵ OCR 복사] 버튼(단축키 `Ctrl+Enter`)을 둔다. OCR 텍스트를
입력창으로 그대로 가져오므로, 검수자는 **다른 부분만 고치면 된다** — 전체를
다시 칠 필요가 없다.

> **자동으로 미리 채우지 않는 이유** — 입력창에 OCR 텍스트가 처음부터 들어가
> 있으면 가장 빠르지만, 읽지 않고 그대로 제출해도 "OCR이 완벽하다"는 데이터가
> 쌓인다. §4.2에서 일괄 패스에 "전체 선택" 버튼을 의도적으로 두지 않은 것과
> 같은 이유로, 한 번의 클릭(또는 키 입력)을 남겨 "이 건을 봤다"는 전제를
> 지킨다. 검수 속도가 여전히 부족하면 §7에서 재검토한다.

**차이 표기법**

검수자가 대괄호를 직접 입력하는 게 아니다. 제출된 텍스트와 그때 화면에 보인
OCR 텍스트를 **서버가 글자 단위로 비교해서 자동 계산**한다.

| OCR 텍스트 | 검수자 입력 | 기록되는 표기 | 차이 |
|---|---|---|---|
| 공부 | 거짓 | `[거짓]` | 2자 (전체 오인식) |
| 사람대 매너가 있어야한다 | 사람 매너가 있어야한다 | `사람[대] 매너가 있어야한다` | 1자 (OCR에만 있던 글자) |
| 사람 | 사랑 | `사[랑]` | 1자 (한 글자 오인식) |

세 가지 종류를 구분해서 저장하고, 화면에서는 색으로도 구분한다.

- `replace` — OCR이 다른 글자로 잘못 읽음 (노란색)
- `delete` — OCR에만 있는 글자, 실제로는 없음 (빨간색 취소선)
- `insert` — OCR이 빠뜨린 글자 (초록색)

입력창 아래에 **입력하는 동안 실시간으로** 표시된다. 단, 화면의 미리보기는
어디까지나 미리보기이고, **DB에 저장되는 값은 항상 서버가 계산한 것**이다 —
화면이 보낸 값을 그대로 저장하면 화면 버그나 조작으로 "차이 없음"이 쌓여도
알아챌 방법이 없고, 이 값은 §6 분석의 근거 데이터이기 때문이다.

**저장 구조와 활용**

| 컬럼 | 용도 |
|---|---|
| `ocr_text_snapshot` | 검수 시점의 OCR 텍스트. **서버가 mielin에서 직접 읽어 저장한다** — 화면이 보낸 값을 믿지 않는다(2026-08-24, §8) |
| `ocr_diff` (JSON) | 차이 구간 목록 — `{op, ocr, typed, ocr_pos, typed_pos}` |
| `ocr_diff_char_count` | 차이 글자 수 요약 (필터·정렬용, 인덱스 있음). **세 상태를 구분한다 — 아래 참고** |

**`ocr_diff_char_count`의 세 상태 (2026-08-24 명문화)** — 이 값은 `NULL`과 `0`이
서로 다른 뜻이며, 뭉쳐서 다루면 안 된다:

| 값 | 의미 |
|---|---|
| `NULL` | **비교하지 않았다** — 판독 불가처럼 비교 대상이 아예 없거나, diff 도입(2026-08-21) 이전에 남긴 데이터 |
| `0` | 비교했고 한 글자도 다르지 않았다 |
| `> 0` | 차이가 있었다 |

`0`인 경우 서버가 `review_type`을 패스로 바꾸므로(§5.5), **타이핑으로 남은
레코드에서 이 값이 `0`인 경우는 사실상 없다.** 즉 타이핑 레코드의 `NULL`은
항상 "비교 안 함"이다. 화면이 `NULL`과 `0`을 truthy 하나로 뭉쳐 둘 다 "OCR과
동일"로 표시하던 버그가 있었다 — **판독 불가 건에 "OCR과 동일"이 뜨는 모순**이
생겼고(판독 불가는 읽지 못했다는 뜻이므로 OCR과 같다고 말할 수 없다),
2026-08-24에 아래처럼 상태별로 나눠 표시하도록 고쳤다:

- 차이 있음 → `OCR과 다른 부분 N자: …`
- 판독 불가 → `비교 대상 없음 (판독 불가)`, 본문도 `-`가 아니라 `판독 불가`
- diff 도입 이전 데이터 → `차이 미기록 (2026-08-21 이전 데이터)`

`ocr_text_snapshot`을 함께 저장하는 이유는 `vlm_model`을 스냅샷으로 두는 것과
같다 — 원본은 다른 DB(`mielin`)에 있어 SQL JOIN이 불가능하고, OCR이 나중에
갱신되면 "그때 무엇과 비교했는가"를 재현할 수 없다. 이 컬럼이 있어야 diff를
나중에 재계산·검증할 수 있고, 두 DB를 오가지 않고 §6의 분석 쿼리를 쓸 수 있다.

세그먼트를 JSON으로 남기므로 "OCR이 '람'을 '랑'으로 읽은 사례 전체"처럼 구체적인
오인식 패턴을 뽑아낼 수 있다. Admin 화면에는 **[OCR과 다른 것만]** 필터가 있고,
API는 `diff_only` / `diff_min_chars`(차이 글자 수 하한)를 지원한다.

**수정할 때** — diff는 수정된 텍스트와 **최초 저장된 스냅샷**을 다시 비교해
계산한다. 스냅샷 자체는 갱신하지 않는다(비교 기준이 최초 검수 시점으로
고정되어야 하기 때문). 수정 직전의 diff는 `ocr_review_edits`에 그대로
보존된다 — §1의 원본 보존 원칙과 같다.

### 5.4 검수자 코멘트 제거 (2026-08-21)

검수자 의견 "검수자 코멘트는 없어도 된다"에 따라 타이핑 패널의 코멘트 입력칸을
없앴다. 난이도(§5.1)와 차이 기록(§5.3)이 코멘트가 담당했던 "왜 다른가"를 대체
하기 때문이다.

`ocr_review_comments.comment` 컬럼은 남겨둔다 — 과거에 입력된 코멘트가 사라지면
안 되고(§1), admin 화면에서는 그 값이 있으면 계속 보여준다. **admin의 참고
코멘트(§4.5)는 전혀 다른 기능이라 그대로 유지된다.**

(자세한 스키마는 `08_add_review_difficulty_level.sql`,
`09_add_review_negative_expression.sql`, `10_add_ocr_diff.sql` 참고)

## 6. 로우데이터 (참고)

로우데이터는 이 시스템이 미리 걸러서 만들어두는 결과물이 아니다. 이 시스템은 검수자들의
의견(패스 / 타이핑+난이도+부정 표현 여부+OCR과의 차이)과 admin 코멘트를 빠짐없이
쌓아두기만 하고, 그중 무엇을 AI 개선 작업에 실제로 가져다 쓸지는 미리 정하지 않는다.

데이터가 충분히 쌓인 뒤, admin 또는 개발자가 그 시점에 필요한 기준으로 직접 DB를 조회해서
판단한다. 예를 들어:

- 두 검수자 모두 난이도 5(판독 불가)로 매긴 것만 제외하고 싶을 때
- 특정 난이도 구간(예: 4~5, 판독이 어려웠던 것)만 뽑고 싶을 때
- 부정 표현이 포함된 것만 모아서 별도로 검토하고 싶을 때
- **OCR이 특정 글자를 특정 글자로 잘못 읽은 사례만 뽑고 싶을 때** (§5.3의
  `ocr_diff` JSON에서 `{ocr:'람', typed:'랑'}` 같은 세그먼트를 조회)
- **차이가 1글자뿐인 것만 / 차이가 큰 것만** 골라 보고 싶을 때
  (`ocr_diff_char_count`)
- 검수자 간 의견이 갈린 것만 우선적으로 검토하고 싶을 때
- `vlm_model`별로 묶어서, 모델이 바뀐 전후로 난이도 분포가 어떻게 달라졌는지
  추이를 비교하고 싶을 때 (난이도 도입 이전 데이터는 난이도가 없으므로 따로
  비교해야 함, §5.1 참고)

이런 기준은 그때그때 다르게 나올 수 있으므로, 이 시스템은 특정 필터링 로직을 고정해서
미리 만들어두지 않는다.

## 7. 아직 결정하지 않은 것

- **일치/불일치 판정 기준** (§4.5): **판정 기능은 2026-08-27에 코드에서
  제거됐다** — 옛 기준(난이도값 비교 + 패스는 전부 일치)이 실제 불일치 3건 중
  1건만 잡았고 어느 화면도 쓰지 않았기 때문이다. 결정해야 할 것은 그대로
  남아 있다: 관리자 중재 기능(§4.5의 난이도 판정)을 실제로 쓰기 시작하면
  **중재 대기열**이 필요하고, 그때 기준을 정해 새로 구현해야 한다. 후보는
  ①난이도가 다르면 불일치(패스 포함) ②판정 종류가 다르면 불일치(타이핑 vs 패스)
  ③둘 다. 그동안 화면에는 완료 여부(완료 n/2)만 표시한다.
- **중재 대기열을 어떻게 노출할까** (§4.5, 2026-08-24 신규): 관리자가 난이도로
  중재할 수 있게 됐지만, "아직 중재하지 않은 불일치 건"을 찾는 방법이 없다.
  전수 검사가 끝나면 불일치가 수천 건 규모가 될 수 있어 목록에서 눈으로 찾는
  방식은 통하지 않는다. 검토한 안: **프리셋 버튼**(`전체` / `중재 대기열` /
  `중재 완료` — 누르면 내부 필터가 세팅됨, 필터바가 늘지 않음) vs **전용
  3단 드롭다운**(`무관` / `코멘트 있음` / `코멘트 없음`). 프리셋 쪽이 필터
  개수를 늘리지 않아 유리하지만, 불일치 판정 기준(위 항목)이 먼저 정해져야 한다.
- **개선불가/개선가능 그룹 재정의** (§5.1, 2026-08-21 신규): 분류 체계가
  없어지면서 검수자가 더 이상 "이 오류가 필기 문제인지 AI 문제인지"를 직접
  고르지 않는다. 지금은 필요하면 난이도 5("판독 불가")를 개선불가에 근사시키는
  안을 문서에 적어뒀을 뿐 실제로 그렇게 쓸지, 세부 원인 분석(예: "비슷한 글자
  오인식"처럼 구체적인 실패 유형)이 계속 필요한지는 검수 데이터가 쌓인 뒤
  판단한다. 필요해지면 §5.1의 난이도 체계에 선택적 세부 원인 필드를 추가하는
  방향도 검토 대상이다.
- **완료 후 수정을 막을 것인가** (§4.3): 지금은 허용하고 `was_completed = 1`로
  기록하며, 본인 카드의 수정 횟수 옆에 "완료 후 수정 포함"이라고 작게
  표시된다(별도 경고 배너는 아니다). 완료 후에도 상대 내용 자체는
  공개되지 않고(§4.4, 2026-08-21), **"이미 완료됐다"는 사실조차 검수자
  화면에서 알 수 없도록 배지의 숫자까지 없앴으므로**(§4.4, 같은 날 추가
  변경), "상대 의견을 보고/알고 맞춰 고친다"는 위험은 실질적으로 사라졌다.
  `was_completed = 1`은 이제 순수하게 **사후 분석용 표시**로만 의미가 있다
  — 검수자가 그 시점에 뭔가를 알고 고쳤다는 뜻이 아니라, 그저 "이 수정이
  상대방 제출 이후에 일어났다"는 타이밍 기록일 뿐이다.
- **[OCR 복사]가 판단을 무디게 하는가** (§5.3, 2026-08-21 신규): OCR 텍스트를
  가져와 고치는 방식은 백지에서 옮겨 적는 것보다 훨씬 빠르지만, 눈에 잘 안 띄는
  오류(조사 한 글자, 비슷한 모양의 글자)를 그냥 지나칠 위험이 있다. 그러면 측정된
  OCR 정확도가 실제보다 좋게 나온다. 지금은 자동 채움을 하지 않고 버튼/단축키로
  한 번의 동작을 남겨 완화했지만, 실제로 이 편향이 생기는지는 데이터로 확인해야
  한다 — 예를 들어 [OCR 복사]를 쓴 건과 직접 입력한 건의 차이 발견율을 비교하는
  방법이 있다(그러려면 어느 쪽을 썼는지도 기록해야 한다. 지금은 기록하지 않는다).
- **검수 속도가 여전히 부족한가** (§5.3): [OCR 복사]로도 부족하면 입력창에
  OCR 텍스트를 처음부터 채워두는 방식이 다음 후보다. 위 편향 항목과 맞물리는
  결정이라 함께 판단해야 한다.
- **판독 불가를 정식 상태로 승격할 것인가** (§5.1/§4.5, 2026-08-24 신규):
  판독 불가는 "OCR이 맞다"(패스)도 "OCR이 틀렸다"(타이핑)도 아닌 **제3의
  판정**인데, DB에는 여전히 `review_type = 'transcription'`으로 저장되고
  `typed_text` 없음으로만 구별된다. 2026-08-24에 **화면 표시(§5.3)와 admin
  목록 필터(§4.5)는 분리를 마쳤다** — 파생 규칙은 `main.py`의
  `_is_typed`/`_is_unreadable` 두 헬퍼 한 곳에 모아뒀다. 남은 두 곳은:
  - **진행 현황 요약** — `SELECT_PROGRESS_SUMMARY`가 `review_type='transcription'`
    으로 세므로 판독 불가가 "타이핑"에 포함된다. 목록 필터와 기준이 어긋난다.
  - **§6 분석 쿼리** — `review_type`만 보고 뽑으면 "OCR이 틀렸다"에 판독 불가가
    섞인다. 분석할 때 `typed_text IS NULL`을 반드시 함께 봐야 한다.

  `review_type`에 `unreadable`을 추가하면 이 둘이 자동으로 해결되지만
  스키마·마이그레이션·집계까지 번지므로, §6 분석을 본격적으로 시작할 때
  판단한다.
- **계정 관리 화면**: admin 역할과 계정 등록 방법(§8 참고)은 정해졌지만, 웹에서 계정을
  추가/비활성화하는 UI는 아직 없다. 지금은 시딩 스크립트를 직접 실행한다.
- **검수 인력 규모**: 완료 조건이 "독립된 2명"인데 현재 annotator 계정이 정확히 2명
  (technonia01/02)이다. 그래서 §3의 "공유 큐 — 사전 할당 없이 누구든 처리 가능"이
  실질적으로 작동하지 않는다. **모든 레코드를 두 사람이 전부 처리해야만** 완료된다
  (검수 입력 자체가 멈추는 건 아니지만, 한 명이 빠지면 어떤 레코드도 completed에
  도달하지 못한다). admin도 검수에 참여할 수 있어 최대 3명이다.
  전수 44,823건 × 2명 = 약 90,000 처리 건이므로, 일괄 패스(§4.2)로 건당 시간을
  줄였더라도 인력 증원은 여전히 검토 대상이다.
- **비밀번호 정책**: `ocr_reviewers`에 `failed_login_count` / `locked_at` 컬럼은 있고
  실패 횟수도 쌓이지만, 몇 회에 잠글지·어떻게 풀지는 아직 정하지 않았다. 비밀번호
  변경 화면도 없다.
- **admin 진행중 레코드 공개 범위** (§4.5): 현재는 첫 의견만으로 내용을 공개한다.
  admin이 검수자를 겸하는 동안은 이론상 독립성이 깨질 수 있으므로, 문제가 되면
  "레코드 완료 또는 admin 본인 의견 있음"으로 되돌릴 수 있다.
- **성능**: 검수 데이터가 쌓이면서 "미검토만 보기" 필터 성능을 어떻게 유지할지.
  지금은 "내가 처리한 키를 최대 20,000건 가져와 `NOT IN`으로 제외"하는 단순한 방식이라,
  그 규모에 도달하면 별도 상태 테이블로 전환해야 한다 (스키마는 그렇게 바꿀 수 있도록
  설계되어 있다 — `v_sct_review_status` 뷰의 주석 참고). admin 열람 화면의 스캔
  (1,000건 상한)도 같은 종류의 과제인데, 여기엔 **검수자 화면과 달리 잘렸다는
  경고가 없다** — 상한을 넘으면 오래된 레코드가 조용히 빠진다. 현재 데이터가
  91건이라 당장 문제는 없지만, 늘어나기 전에 경고를 붙이거나 스캔 자체를
  페이지네이션해야 한다.

## 8. 변경 이력 (초안 → 현재)

초안(로그인 없이 설계했던 버전) 이후 구현하면서 결정·변경된 사항이다.

| 항목 | 초안 | 현재 |
|---|---|---|
| 검수자 신원 | 미정 — 화면에서 "검수자 A/B"를 직접 선택하는 임시 방식 | `ocr_reviewers` 계정으로 로그인, 세션의 `reviewer_id`가 신원. 화면에서 선택 불가 |
| 로그인 아이디 | `email` 컬럼 | `username` 컬럼 신설 (`04_add_reviewer_username.sql`). 실제 계정이 이메일 형식이 아니라서 분리 |
| admin 계정 관리 | **미정** (§7 항목) | `role='admin'`으로 구분, `scripts/seed_reviewers.py`로 등록. 웹 UI는 아직 없음(§7에 남김) |
| admin 권한 | 문서상 역할만 정의 | 서버가 실제로 강제 — `/admin`과 `/api/ocr/admin/*`은 403 |
| 블라인드 원칙 | 원칙만 서술 (누가 강제하는지 미명시) | 서버가 강제. 제출 전에는 `review_count`/`status`까지 숨김 |
| "미검토만 보기" | 요구사항만 존재 | 구현 완료. 두 DB 분리 때문에 `NOT IN` 방식 + 상한 경고 |
| 분류 선택 시점 | "2명 완료 후"로 읽히는 서술 | 타이핑 제출과 동시 (§4.4 구현 메모). 확인 대상으로 §7에 남김 |
| 불일치 판정 | "패스 vs 타이핑, 또는 분류 그룹" | **미구현** — 옛 구현(난이도 비교)이 실제 불일치를 놓쳐 2026-08-27에 제거. 기준 결정은 §7 |
| 오류 응답 구분 | 미명시 | 중복 제출 409 / FK 위반 400으로 분리 |
| admin의 검수 참여 | "독립 의견을 내지 않음" | **허용.** 인력이 적어 admin도 검수자 겸임. 의견의 무게는 다른 검수자와 동등 |
| admin 열람 범위 | 완료된 레코드만 | 진행중 레코드도 목록에 표시. **의견이 하나라도 있으면 내용 공개** (2026-08-19부터, §4.5). 이전엔 완료되거나 admin 본인 의견이 있어야만 공개했음 |
| admin 진행 현황 | 없음 | 완료/진행중 건수 요약 추가 (검수자별 통계는 불필요하다는 판단으로 제외). 전체 SCT 검사 수·검사자 수 카드도 추가. **완료/진행중 건수는 패스만 있는 레코드를 제외**(2026-08-20) — 목록의 개별 배지(n/2)는 패스 포함 그대로라 기준이 다름 |
| 목록 보기 방식 | 리스트 1건씩 | **그리드 카드 격자 하나로만.** 전수 검사 처리량 확보 목적 (한때 그리드/리스트 전환이었으나 리스트는 2026-08-21에 제거 — 아래 "리스트 보기 모드" 항목) |
| 일괄 패스 | 없음 (1건씩) | **추가.** 그리드에서 체크한 건을 한 번에 패스. 1회 50건 상한, "전체 선택" 버튼 없음 |
| 화면 수 | 3개 (SCT 데이터 조회 / 검수 / Admin) | **2개.** "SCT 데이터 조회"는 검수 화면과 역할이 겹쳐 없애고, 고유 기능(SCT 질문 컬럼·VLM 상태 필터)만 검수 화면으로 이전 |
| 내 처리 상태 필터 | "미처리만" 체크박스 | 4종 드롭다운 (미처리 / 내가 타이핑 / 내가 패스 / 전체) |
| 이미지 없는 레코드 | 목록에 포함 ("이미지 준비 중"으로 표시) | **제외.** 검수 대상이 아님 |
| 본인 타이핑 수정 | 없음 (제출 후 변경 불가) | **가능.** 단 본인 것만, 패스는 제외, 수정 직전 값은 `ocr_review_edits`에 보존 (§1 준수) |
| 완료 후 상대 의견 비교 화면 (§4.4) | 완료되면 OCR/검수자1/검수자2를 나란히 비교 표시 | **검수자 화면에서 제거** (2026-08-19). 실사용 중 "다른 검수자 의견을 볼 필요가 없다"는 피드백. 비교는 admin 화면(§4.5)에서만 제공 |
| admin 코멘트 | 참고용 메모만, 분류 없음, 여러 건 누적 가능 | **분류 추가 + 레코드당 1건으로 변경, 수정 가능** (2026-08-19). 검수자 본인 수정(§4.3)과 같은 방식·같은 스타일. "검수자 의견" 칸에도 `(코멘트)` 표시로 같이 노출 |
| 타이핑 입력창 | [타이핑] 버튼을 눌러야 펼쳐짐, 텍스트 없으면 분류 선택 불가 | **[타이핑] 버튼 삭제, 입력창 항상 펼쳐짐** (2026-08-20). **텍스트 없이 분류만으로도 제출 가능** — "읽기 불가"/"텍스트 아님"처럼 옮겨 적을 내용이 없는 경우 대응. 텍스트만 지우고 분류를 유지하면 패스로 전환되지 않음 |
| 검수자 분류(9종) | 타이핑 시 개선불가/개선가능 세부유형 드롭다운 선택 (§5 원안) | **OCR 난이도(1~5)로 완전 대체** (2026-08-21). 실제 검수자 의견 — "드롭다운보다 난이도 입력이 빠르다" — 반영. 타이핑 처리에서 항상 필요(더 이상 "OCR과 다를 때만"이 아님). 과거 분류 데이터는 삭제하지 않고 그대로 조회 가능, admin 참고 코멘트의 분류(별개 기능)는 영향 없음 (§5.1) |
| 부정 표현 표시 | 없음 (`ai_error_negation` 분류로만 간접 확인 가능했음) | **신설.** 죽음·자살·우울 등 키워드가 OCR 텍스트에 있으면 자동으로 미리 체크된 체크박스를 패스/타이핑 공통으로 제공. 검수자가 직접 켜고 끌 수 있음. 키워드는 `ocr_negative_keywords` 마스터 테이블로 관리 (§5.2) |
| 텍스트 입력 방식 | 백지에서 전부 직접 입력 | **[⤵ OCR 복사] 버튼 + `Ctrl+Enter` 단축키 추가** (2026-08-21). OCR 텍스트를 가져와 다른 부분만 고치면 됨. 자동 채움은 하지 않음 — 읽지 않고 제출하는 것을 막기 위해 한 번의 동작을 남겼다 (§5.3, 편향 우려는 §7에 남김) |
| OCR과의 차이 | 기록하지 않음 (`typed_text` 전문만 저장) | **신설.** 글자 단위 차이를 서버가 자동 계산해 `ocr_diff`(JSON)·`ocr_diff_char_count`로 저장하고, `ocr_text_snapshot`도 함께 남김. 입력 중 실시간 표시 + admin의 [OCR과 다른 것만] 필터 + `diff_only`/`diff_min_chars` API (§5.3) |
| 검수자 코멘트 | 타이핑 시 자유 코멘트 입력 가능 | **입력칸 제거** (2026-08-21). 난이도와 차이 기록이 "왜 다른가"를 대체. 컬럼과 과거 데이터는 유지하고 admin 화면에서는 계속 표시. admin 참고 코멘트는 별개 기능이라 영향 없음 (§5.4) |
| 완료 후 상대 의견 공개 범위 (§4.4) | 화면(review.js)에서만 본인 것만 그림. API 응답에는 완료 시 양쪽 내용이 다 담겼음(개발자도구로 보면 새어나감) | **API 응답에서도 제외** (2026-08-21). "검수자 간에 서로 확인할 필요가 없다"는 판단으로 `_blind_state`가 완료 후에도 본인 것만 반환하도록 강화. admin 화면은 별도 엔드포인트라 영향 없음 |
| 패스의 난이도 (§5.1/§4.2) | 패스는 난이도 없음(0클릭 유지) | **패스도 난이도(1~4)가 필수로 확장** (2026-08-21). "패스도 난이도 없이는 처리되지 않아야 한다"는 판단. 판독불가(5)는 패스와 모순이라 제외. 일괄 패스도 카드별로 난이도를 실어 보내야 하며, 미선택/판독불가 카드는 제외되고 몇 건이 빠졌는지 안내됨 |
| 본인 의견 수정 범위 (§4.3) | 타이핑만 수정 가능, 패스는 수정 대상 아님(400) | **패스도 수정 대상으로 확장** (2026-08-21). review_type을 고정된 값이 아니라 typed_text/난이도로 매번 다시 계산 — 패스를 수정하며 텍스트를 채우면 그 순간 타이핑으로 분류되고("패스" 표시가 사라짐), 타이핑에서 텍스트를 지우면 패스로 분류된다(양방향). 판독불가(5)와 텍스트가 동시에 있는 조합은 여전히 거부(400) |
| 완료 배지 표기 (§4.4) | "완료 (n/2)" — 상대가 몇 명 처리했는지 숫자로 표시 | **숫자 제거, "완료"만 표시** (2026-08-21). "완료 (1/2)"가 아직 안 끝났는데 완료라고 읽혀 혼란스러웠고, 검수자 화면은 상대 진행 상황을 알 필요가 없는 독립 운영 화면이라는 판단. `review_count`/`status` 필드를 API 응답에서도 제거(§4.4) — 화면뿐 아니라 서버 응답 자체에서 숫자가 안 내려온다. 진행 현황 집계는 admin 화면(§4.5)의 역할로 그대로 남음 |
| 그리드 단독 패스 (§4.2) | 카드 하나만 패스하려도 체크박스로 선택 후 하단 일괄 버튼을 눌러야 했음 | **"카드 체크 + [제출]"로 단건 패스** (2026-08-21). 한때 카드마다 별도 [패스] 버튼을 뒀지만, 그 버튼이 체크 상태를 무시하는 두 번째 진입점이 되어 혼란스럽다는 의견을 반영해 없애고, 이미 있는 체크박스+[제출] 조합만 남겼다 |
| 검수자 화면의 부정 표현 필터 | 없음 (admin 화면에만 있었음) | **추가** (2026-08-21). "부정 표현 포함" 체크박스로 OCR 텍스트에 키워드가 감지된 레코드만 조회 — mielin 쿼리에 `ocr_text LIKE` 조건을 동적으로 추가. 2026-08-24에 필터바 맨 앞으로 옮기고 라벨에서 "만"을 뺐다(체크박스는 조합 조건이라 "~만"이 배타적으로 읽히는 게 부정확했다). **2026-08-31에 라벨을 다시 "부정 표현만"으로 되돌렸다** — 실제 필터 로직은 처음부터 "부정 표현이 감지/확정된 것만" 골라내는 배타적 조건(OR로 두 기준을 합치되 결과는 항상 "그 중 하나만 있어도 포함"이 아니라 "부정 표현이 있는 것만")이었는데, "포함"이라는 이름이 "그 외에도 포함해서 보여준다"로 오독되기 쉬웠다. 로직은 바뀌지 않았고 이름만 실제 동작에 맞게 고쳤다 — admin 화면(§4.5)의 같은 체크박스도 동일하게 바꿨다 |
| 리스트 보기 모드 | 그리드/리스트 전환 가능, 리스트는 이미지를 크게 보는 화면 | **완전 제거** (2026-08-21, 위 "그리드 단독 패스" 항목과 같은 날). 그리드에서도 이미지 클릭 시 원본 크기 모달이 열려 리스트의 핵심 기능(큰 이미지)이 이미 커버됐고, 단건 패스 경로가 그리드에도 생기면서 "단건 빠른 처리"라는 리스트의 남은 장점도 사라짐. 모드 토글 버튼·`renderList()`·관련 CSS를 모두 삭제 |
| 그리드 카드 크기 (§4.1) | 1920×1080에서 한 줄에 5장 (카드 최소 폭 300px) | **한 줄에 4장으로 축소, 카드 최소 폭 420px** (2026-08-24). 5장일 때 OCR 텍스트와 난이도 버튼이 너무 작아 읽기 어렵다는 피드백. 카드를 키운 만큼 내부 요소도 함께 확대(OCR 텍스트 16→18px, 질문 15→16px, 난이도 버튼 32→38px, 이미지 최대 높이 144→220px) |
| 부정 표현 체크박스 위치 (§4.2) | 난이도 버튼 옆(타이핑 패널 안) | **카드 우측 상단(카드 헤더)으로 이동** (2026-08-24). 이미 처리한 건은 타이핑 패널이 접혀 있어 값이 안 보였다. 헤더로 올려 접힘 여부와 무관하게 항상 보이게 함. 라벨도 "⚠ 부정 표현"으로 줄이고 전체 설명은 툴팁으로 |
| 로그인 후 첫 화면 (§4.0) | 역할과 무관하게 `/`→`/review` (검수 그리드), admin은 상단 "Admin 열람" 링크로 이동 | **역할별 분기** (2026-08-24). `role='admin'`이면 `/admin`, 그 외는 `/review`. admin은 열람이 주 업무라 그 화면이 사실상 홈이고, 그래서 "Admin 열람" 탭 링크도 삭제 |
| admin 목록 기본 필터 (§4.5) | "텍스트 입력한 것만" 체크박스가 **기본 체크** | **기본값을 "타이핑한 것" 드롭다운으로 재구성** (2026-08-24). 난이도·부정 표현이 패스에도 붙게 되면서 "타이핑 여부"만 보는 기존 필터로는 패스 건을 아예 볼 수 없었다. 배타적 상태(타이핑한 것/OCR과 다른 것/패스한 것/전체)는 드롭다운으로 묶고, 부정 표현은 독립 체크박스로 분리해 조합 가능하게 함 |
| admin의 "패스한 것" 정의 (§4.5) | 없음 (패스만 있는 레코드를 골라낼 방법이 없었음) | **신설** (2026-08-24). "한 명이라도 패스"가 아니라 **"타이핑한 사람이 아무도 없음"**으로 정의 — 두 명 다 패스한 건과 한 명만 패스하고 나머지가 미처리인 건이 모두 포함된다. `pass_only` 쿼리 파라미터 |
| 공유 CSS 클래스 스코프 | `.my-review`/`.edit-btn`을 검수자 카드와 admin 목록이 그대로 공유 | **검수자 카드용 확대는 `.review-card` 하위로만 한정** (2026-08-24). 카드 가독성 개선이 admin 테이블에도 번져 레이아웃이 틀어지는 문제가 있었다. 기본값은 admin 기준(13~14px)으로 두고 그리드 카드에서만 키운다 |
| admin 진행 현황 카드 (§4.5) | ① 패스만 있는 레코드를 집계에서 빼서 실제보다 훨씬 작게 나왔다(진행중 25 vs 실제 97) — 카드와 목록 전체 건수가 어긋났다. ② 검수 대상 이미지가 몇 장인지 볼 수 없었다 | ① **패스도 "처리한 것"으로 센다** (2026-08-24). 검수자가 이미지를 보고 "OCR이 맞다"고 판정한 것도 분명히 처리한 것이다. 이제 `완료 + 진행중 = 검수 기록이 있는 전체 레코드`가 되어 목록 건수와 정확히 맞는다(검증: 7 + 97 = 104 = 목록 전체). ② **전체 이미지 수 카드 신설** — 검사자 수 다음에 놓았다. `s3_key`가 있는 레코드만 세므로 목록의 `has_image` 필터와 같은 기준이다(빈 레코드는 검수 대상이 아니다) |
| 수정 시 비교 기준이 비어 있던 문제 (§5.3) | 수정(PATCH)은 저장된 `ocr_text_snapshot`과 다시 비교하는데, **스냅샷 도입(2026-08-21) 이전 데이터는 그 값이 `NULL`**이었다. 비교 대상이 없으니 입력한 글자 전체가 "OCR과 다름"으로 기록됐다 — 화면의 OCR과 한 글자만 달라도 "다른 부분 13자"처럼 표시되어 버그로 신고됐다(74건이 스냅샷 없음, 그중 12건이 증상 노출) | **스냅샷이 비어 있을 때만 원본 DB에서 OCR을 가져와 비교하고, 그 값을 스냅샷에 채운다** (2026-08-24). 이미 값이 있으면 절대 덮어쓰지 않는다(`COALESCE`) — 비교 기준은 최초 검수 시점으로 고정되어야 하기 때문이다. 빈 칸을 메우는 것이라 이 원칙과 충돌하지 않고, 한 번 채워지면 이후로는 정상 동작한다. 신규 제출(POST)에 넣었던 "서버가 직접 조회" 규칙을 수정 경로에도 확장한 것이다 |
| 띄어쓰기를 글자 차이로 세던 문제 (§5.3) | 공백을 한 글자로 비교해서, 띄어쓰기만 다른 입력이 "OCR이 틀렸다"로 집계됐다. 실제로 한 답변을 두 검수자가 `있어야한다` / `있어야 한다`로 적어 **둘 다 차이 13자**로 기록됐다 — 판단은 사실상 같은데 결과가 갈렸다 | **글자 비교에서 공백을 제외하고, 띄어쓰기 차이는 `spacing_diff` 컬럼으로 분리**한다 (2026-08-24, `11_add_spacing_diff.sql`). 위 사례는 차이 **1자**(`대`→`의`)로 바뀌고, 띄어쓰기가 다르면 화면에 "띄어쓰기가 OCR과 다름"이 따로 붙는다. 아예 버리지 않은 이유는 띄어쓰기도 OCR 품질의 일부라 "OCR이 띄어쓰기를 얼마나 틀리는가"를 §6에서 따로 볼 수 있어야 하기 때문이다. 기존 행의 diff는 재계산하지 않는다 — 다시 계산하면 "그때 무엇과 비교했는가"가 바뀌므로, 분석 시 `created_at`으로 구분한다 |
| 난이도 표시의 라벨 (§5.1) | 표시할 때마다 `난이도: 3 (보통)`처럼 라벨을 붙였다 — "보통"은 3에서 파생된 이름일 뿐 추가 정보가 없는데, 여러 항목이 `·`로 이어지는 목록에서 길이만 두 배가 됐다 | **1~4는 숫자만(`난이도 3`), 5는 라벨만(`판독 불가`)** (2026-08-24, `describeDifficulty`). 5에 "난이도" 접두어를 안 붙이는 이유: 5는 "가장 어려움"이 아니라 **종류가 다른 판정**이라 `난이도 5`로 적으면 최상급으로 오해된다(실제로 그 혼동이 반복돼 난이도 필터에서 5를 빼낸 적이 있다). 라벨은 **고르는 UI**(입력 버튼·필터 팝오버·관리자 모달)에만 남겼다 — 그 노출이 숫자↔의미 학습을 계속 만들어주므로 표시에서 빼도 안전하다. 겸해서 검수자 카드의 판독 불가 중복도 줄였다: 본문·diff 줄·메타 줄에 세 번 나오던 것을 **메타 줄에서 생략**해 두 번으로 |
| admin 목록의 설명 과다 (§4.5) | "검수자 의견" 칸에 `난이도 · 차이 6자 · 띄어쓰기 다름`, 관리자 줄에 `· 확정`, 패스에 `(일치)` 등 부가 표현이 겹쳐 한 행을 읽기 어려웠다 | **중복·불필요한 꼬리표 제거** (2026-08-24). ① **글자 차이·띄어쓰기 차이 삭제** — 바로 왼쪽 "OCR / 검수자 입력" 칸이 이미 대괄호 표기로 어디가 달랐는지 보여주므로 같은 정보를 숫자로 반복한 것이었다. ② 관리자 줄의 **`확정` 삭제** — 줄 앞에 `관리자:` 이름이 붙어 누가 남긴 값인지 이미 드러난다(판정의 성격은 문서에 남겼다). ③ `패스 (일치)` → **`패스`** — 패스가 곧 "OCR이 맞다"는 판정이라 같은 말의 반복이었다. 남긴 것: 난이도(값 자체가 정보), 부정 표현의 `(자동감지)`/`(검수자 판단)` 구분(오탐 여부를 알려준다), 배지의 `(2/2)` 진행 숫자 |
| 분류(classification) 체계 완전 제거 | 검수자 분류는 2026-08-21에 난이도로 대체됐고 admin 코멘트도 2026-08-24에 난이도로 바뀌었지만, **컬럼과 마스터 테이블은 "과거 데이터 표시용"으로 계속 남아 있었다** — 어느 화면에서도 쓰이지 않는데 §6 분석에서는 난이도와 분류 두 체계를 함께 고려해야 했다 | **컬럼·테이블·코드·엔드포인트를 모두 삭제** (2026-08-24, `13_drop_classification.sql`). `ocr_review_comments.classification`, `ocr_admin_comments.classification`, `ocr_review_edits.prev_classification`, 마스터 테이블 `ocr_classification_types`, `GET /api/ocr/classification-types` 엔드포인트, admin 필터의 `classification` 쿼리 파라미터가 함께 없어졌다. 불일치 판정(`_resolved_group`)의 분류 하위 호환 분기도 제거됐다. 지워진 값(검수자 16건·admin 3건·마스터 9행, 전부 테스트 데이터)은 `backup/` 아래에 받아뒀다 |
| 관리자 코멘트에 난이도 추가 (§4.5) | 관리자가 남길 수 있는 값이 **분류(classification)** 뿐이었다 — 검수자 쪽은 이미 난이도로 대체됐는데 관리자만 옛 체계에 남아 있었고, 두 검수자가 난이도를 다르게 냈을 때 중재할 방법이 없었다 | **분류 드롭다운 → 난이도 버튼(1~4 + 판독 불가)** (2026-08-24, `12_add_admin_comment_difficulty.sql`). 검수자 난이도와 **다른 컬럼**에 담는다 — 관리자는 두 검수자 의견을 모두 본 뒤 판단하는 **비블라인드 중재값**이라, §1의 독립 판단이 전제인 값과 섞으면 통계가 오염되기 때문이다. 용도는 **불일치 중재**이고, 분석의 "확정 난이도"는 ①관리자 값 ②두 검수자 일치값 ③미확정 순으로 정한다. 완료 판정에는 여전히 영향이 없다. 코멘트는 **선택**으로 바꿨다(난이도만 찍고 넘어가는 경우가 많다) — 다만 둘 다 비면 남길 내용이 없어 400으로 막는다. 입력은 표 아래 인라인 폼 대신 **모달**로 옮겼다: 난이도 버튼이 들어가면서 행 높이가 들쭉날쭉해지고 어느 레코드의 폼인지 헷갈렸기 때문이다. 목록에는 `난이도: 5 (판독 불가) · 확정`처럼 **`확정`을 붙여** 검수자 값과 구분한다 |
| 판독 불가를 조작하는 컨트롤이 둘이던 문제 (§4.1/§5.1) | 판독 불가 제외를 체크박스로 떼어냈는데 **난이도 필터에 5번 버튼이 그대로 남아** 같은 대상을 두 컨트롤이 각각 조작했다. "판독 불가 제외 ✓ + 난이도 5"처럼 서로 반대되는 지시가 가능해 결과가 **항상 0건**이 되는 조합이 있었다 | **난이도 팝오버 안으로 합쳐 한 컨트롤이 전담한다** (2026-08-24). 난이도는 1~4 등급만 다루고, 판독 불가는 같은 팝오버의 **3단 선택(제외 / 포함 / 만 보기)** 이 담당한다. 체크박스가 없어져 필터바 컨트롤이 하나 줄었고, "판독 불가만 보기"도 한 번에 된다. 접힌 버튼 라벨은 **현재 설정을 그대로 말해준다**(`판독 불가 제외`, `난이도 1,2 · 판독불가 제외`) — 기본값이 "제외"라 데이터가 빠진 상태인데 라벨이 중립적이면 무엇이 빠졌는지 모른 채 쓰게 되기 때문이다. "만 보기"일 때는 난이도 버튼을 잠근다(교집합이 비어 0건이 되므로) |
| 검수자 화면의 연령대·VLM 모델 필터 (§4.1) | 검수자 화면에도 있었다 | **제거** (2026-08-24). 검수자는 이미지를 보고 판정만 하므로 모델·연령대로 나눠 작업할 이유가 없다. admin 화면에는 §6 분석(모델별 OCR 품질 비교)에 필요하므로 그대로 둔다 |
| 난이도 필터가 두 가지 일을 겸하던 문제 (§4.1/§5.1) | "등급으로 좁혀보기"와 "판독 불가 제외"를 난이도 필터 하나가 겸했다. 판독 불가를 빼려고 기본값을 1~4로 두자, 난이도가 없는 **미처리 건이 0건이 되어 검수자의 기본 작업 화면이 통째로 비었다**. 상태별로 기본값을 다르게 하는 방안도 검토했으나 그건 겸직의 증상을 관리하는 것일 뿐이었다 | **두 일을 분리한다** (2026-08-24). ① 난이도 필터는 본래 역할만 하고 기본값은 **항상 "전체(1~5) = 필터 미적용"** — 상태가 바뀌어도 기본값이 달라지지 않아 혼동이 없다. ② "판독 불가 제외"는 **독립 체크박스**(기본 켜짐)로 떼어냈다 — 타이핑/패스 어느 상태와도 조합되는 속성이라 부정 표현과 같은 구조다. 미처리 건에는 판정 자체가 없어 자동으로 무해하다. 덕분에 "전체 모드에서 미처리를 항상 포함"시키던 특수 처리도 제거했다 — 기본이 필터 미적용이라 전체는 자연히 다 보이고, 좁혔을 때 처리분만 남는 것이 오히려 기대에 맞는다 |
| 아직 처리하지 않은 것 + 난이도 (§4.1) | 난이도는 내가 남긴 값이라 미처리 건에는 없는데, 필터를 그대로 적용해 항상 0건이 나왔다 | **미처리 상태에서는 난이도를 아예 무시한다** (2026-08-24). 어떤 값을 골라도 47,004건 그대로 나온다 |
| 검수자 화면의 "부정 표현 포함" 필터 (§5.2) | **OCR 텍스트의 키워드만** 보고 걸렀다 — 검수자가 최종 확정한 판단(`contains_negative_expression`)은 아예 보지 않았다. 그래서 ①키워드가 없는 답변에 검수자가 직접 체크해도 이 필터에 안 잡히고, ②자동 감지된 것을 체크 해제해도 계속 잡혔다. 같은 이름의 admin 필터는 검수자 판단 기준이라 **두 화면이 서로 다른 답을 냈다**(검수자 372건 vs admin 1건) | **두 기준을 OR로 합친다** (2026-08-24): `내가 부정 표현으로 확정한 것` **또는** `아직 내가 처리하지 않았고 OCR에 키워드가 있는 것`. 처리 후에는 사람의 판단을 따르고(①② 해결), 처리 전에는 "위험한 답변을 미리 훑어보는" 원래 용도가 그대로 유지된다. 한쪽만 택하지 않은 이유는 미처리 레코드에는 아직 판단 자체가 없어서 판단 기준만으로는 미리 볼 수 없기 때문이다. admin은 처리된 것만 다루므로 판단 기준 하나만 쓴다(변경 없음) |
| admin 목록에 SCT 질문이 없던 문제 (§4.5) | 검수자 카드에는 질문이 보이는데 admin 표에는 **문항 번호만** 있었다. 관리자는 두 검수자의 판독이 갈린 레코드를 보는 자리인데, 질문을 모르면 어느 쪽 판독이 문맥에 맞는지 판단할 근거가 없다 — 번호만으로는 무슨 질문인지 알 수 없어 매번 다른 자료를 찾아야 했다 | **"OCR / 검수자 입력" 칸 맨 위에 질문을 표시** (2026-08-26). 검수자 카드(`.card-question`)와 **같은 위치·같은 스타일**(16px, 3줄 clamp, 전체 문장은 `title`로 넘김 — `.cell-question`)을 쓴다. 서버는 손대지 않았다: admin API가 이미 `sct_question`을 내려주고 있었고 화면만 쓰지 않았다. **2026-08-31에 두 가지를 더 바꿨다** — ① 별도였던 "문항" 칼럼(번호만 있던 자리)을 없애고, 그 번호를 질문 앞에 `[36] 시간을 되돌릴 수 있다면 …`처럼 붙였다(칼럼 하나를 줄이면서 번호와 질문을 한 자리에서 같이 보게 됨). ② 글자색을 회색(`#4b5563`)에서 진한 파란색(`#1d4ed8`)으로 바꿨다 — 바로 아래 OCR 원문과 글자 크기가 같아 구분이 잘 안 된다는 지적을 반영했다(review.js의 `.card-question`도 같은 CSS 규칙을 공유하므로 검수자 화면에도 동일하게 적용됨) |
| 난이도 필터 (§4.1/§4.5) | 드롭다운 단일 선택 — 한 번에 난이도 하나만 볼 수 있었다 | **다중 선택** (2026-08-24). "1,2,3,4를 한꺼번에" / "1,2만" 같은 조회가 가능해졌다. API는 `difficulty_level`을 반복해서 받고(`?difficulty_level=1&difficulty_level=2`), 그중 하나라도 해당하면 통과한다(OR). **아무것도 안 고르면 전체**이며 이는 1~5를 모두 고른 것과 다르다 — 후자는 난이도가 없는 과거 데이터를 조용히 빼버린다. 화면은 알약 버튼 5개를 필터바에 늘어놓지 않고 **버튼 하나에 접어 팝오버로 펼친다**(기간 필터의 `range-popover`와 검수 카드의 난이도 알약을 조합한 것) — 필터바 컨트롤이 이미 많아 가독성이 떨어지기 때문이다. 접힌 버튼 라벨이 선택 상태를 보여준다(`난이도 1, 2` / 3개 이상이면 `난이도 N개 선택`). 두 화면이 `difficulty-filter.js` 하나를 공유한다 |
| 이전 입력 보기 (§4.3) | 수정할 때마다 직전 값 전체를 무제한 누적 저장하고, 검수자 화면에서 펼쳐 볼 수 있었다 | **내용은 저장·표시 모두 중단, 횟수만 남긴다** (2026-08-24). 최종 검수 결과를 얻는 것이 목표이고, 빠르게 입력하다 생긴 오타를 고친 기록은 그 목표에 기여하지 않는다는 판단이다. 화면은 `수정 2회`처럼 횟수만 보여준다(펼침 UI 제거). 다만 두 가지는 남긴다 — **수정 횟수**는 "여러 번 고쳤다 = 검수 기준이 안 잡혔다"는 관리 신호이고, **완료 후 수정 여부**(`was_completed`)는 상대 의견을 볼 수 있게 된 뒤의 수정인지 알려주는, 블라인드가 지켜졌는지 확인하는 유일한 근거다. `prev_*` 컬럼은 과거 데이터가 들어 있어 남겨두되 새로 채우지 않는다. admin 화면에는 원래 이 기능이 없었다 |
| OCR 스냅샷의 출처 (§5.3) | 화면이 보낸 `ocr_text`를 그대로 `ocr_text_snapshot`에 저장 — **diff 계산의 유일한 비교 기준인데 검증이 없었다.** 수정(PATCH)은 `ocr_text`를 받지 않으므로 한번 잘못 들어가면 화면에서는 영원히 고칠 수 없다. 실제로 잘못된 스냅샷이 든 레코드에서 "OCR과 똑같이 입력했는데 전부 다르다고 표시되는" 문제가 발생했다 | **서버가 원본 DB(mielin)에서 직접 다시 읽어 저장** (2026-08-24, `_authoritative_ocr_text`). 단건 제출과 일괄 패스 모두 적용하며, 일괄은 배치 조회 1회로 처리한다. `payload.ocr_text`는 참고용으로만 남기고 **mielin에 닿지 못할 때만** 그 값으로 물러난다(검수 자체를 막는 것보다 낫다). 클라이언트가 보낸 diff를 믿지 않는 것과 같은 이유다 — 이 값은 §6 분석의 근거라 단일 출처여야 한다 |
| 판독 불가 선택 시 입력창 (§4.3) | 판독 불가 버튼은 난이도 버튼끼리만 서로 껐고 **입력창은 그대로 열려 있었다** — 텍스트를 입력한 뒤 판독 불가를 누르면 모순 상태가 만들어지고, [제출]을 눌러야 비로소 에러가 났다 | **판독 불가를 누르는 순간 입력창을 잠근다** (2026-08-24). 난이도 1~4를 다시 고르면 풀린다. 이미 친 텍스트는 **지우지 않고 회색으로 남긴다** — 실수로 눌러도 되돌리면 그대로 살아나기 때문. 잠긴 값은 저장되지 않으며(제출 시 `typed_text`를 아예 안 보냄) 그 사실을 안내 문구로 함께 보여준다. 기존 판독 불가 건을 [수정]으로 열 때도 잠긴 상태로 그린다. 규칙(텍스트+판독 불가 저장 불가)은 그대로이고 **막는 시점만 앞당긴 것**이라, 서버의 400 검증은 그대로 둔다 |
| 부정 표현의 출처 구분 (§5.2) | admin은 `contains_negative_expression`만 보고 "⚠ 부정 표현"으로 표시 — 시스템이 자동 감지한 것인지 검수자가 직접 판단한 것인지 구별할 수 없었다 | **"⚠ 부정 표현 (자동감지)" / "(검수자 판단)"으로 구분 표시** (2026-08-24). 자동 감지 결과는 저장돼 있지 않으므로 서버가 **검수 당시 `ocr_text_snapshot`으로 다시 계산**해 내려준다(지금 OCR로 계산하면 그 사이 OCR이 갱신된 경우 당시와 다른 답이 나온다). 검수자가 자동 감지를 **해제한 건은 admin에 표시하지 않는다** — 부정 표현 단어가 들어 있다고 해서 모두 부정 표현인 것은 아니고, 그 판단은 사람이 한 것이 맞기 때문이다 |
| "텍스트 + 판독 불가" 서버 검증 (§4.3) | `PATCH`(수정)에만 있고 **`POST`(신규 제출)에는 빠져 있었다** — 화면(review.js)이 막아주는 동안에만 안전했고, API를 직접 호출하면 "판독 불가인데 텍스트가 있는" 모순 행이 그대로 저장됐다 (실제로 재현 확인) | **`POST`에도 같은 검증 추가** (2026-08-24, 400). 블라인드와 같은 원칙이다 — 화면에서만 막으면 개발자도구/직접 호출로 뚫린다. 이 불변식이 깨지면 난이도 5로 판독 불가를 골라내는 필터(§4.5)와 화면 표시(§5.3)가 함께 어긋난다. `PATCH` 쪽 주석은 "submit_review와 같은 규칙"이라고 적혀 있었지만 실제로는 한쪽에만 있었다 |
| admin 목록의 판독 불가 (§4.5) | "타이핑한 것"에 섞여 나와 구별 불가 (`review_type`만 봤음) | **드롭다운에 "판독 불가" 신설, 타이핑에서 분리** (2026-08-24). "타이핑한 것"은 `typed_text`가 있는 것만으로 좁히고(`typed_only`), 판독 불가는 `unreadable_only`로 따로 조회한다. 판정 기준은 난이도 5가 아니라 "transcription인데 텍스트 없음" — 난이도 5만 보면 난이도 도입 이전 레거시 5건이 세 필터 어디에도 안 걸려 "전체"에서만 보이는 사각지대가 생겼다. 이 정의로 타이핑/판독불가/패스가 레코드 전체를 빠짐없이 나눈다(검증: 10+13+74 = 97 = 전체). 진행 현황 요약은 여전히 `review_type` 기준이라 기준이 다르다(§7) |
| 판독 불가의 차이 표시 (§5.3) | `ocr_diff_char_count`가 falsy면 무조건 "OCR과 동일" — 판독 불가 건에도 그렇게 떴다 | **`NULL`/`0`/`>0` 세 상태로 분리 표시** (2026-08-24). 판독 불가는 "비교 대상 없음 (판독 불가)", diff 도입 이전 데이터는 "차이 미기록"으로 구분한다. 판독 불가는 읽지 못했다는 뜻이라 OCR과 같다고 말할 수 없는데 정반대로 표시되던 모순이었다. `review_type`은 그대로 두고 **표시 계층만** 고쳤다 — 판정 자체는 `typed_text` 없음 + 난이도 5로 이미 판별 가능하기 때문. 수정 이력 패널(`editHistoryPanel`)은 원래부터 타이핑/판독 불가/패스 3분기로 옳게 처리하고 있었고, 현재값 패널만 2분기여서 어긋나 있었다 |
| 검수자 카드 패스 체크박스 기본값 (§4.2) | 기본은 미체크 — 패스할 건마다 검수자가 매번 직접 체크해야 했다 | **목록을 불러올 때 기본으로 체크된 채 그려진다** (2026-08-31). 대부분 OCR이 그대로 맞아서 매번 체크하는 반복 클릭이 컸다는 의견을 반영했다. "전체 선택" 버튼(위 §4.2 방어 장치 참고)과는 다르다 — 난이도는 여전히 카드마다 직접 골라야만 제출되므로, 체크가 미리 되어 있어도 이미지를 안 보고 그냥 넘길 수는 없다. 타이핑을 시작하면 그 카드의 체크는 기존 로직 그대로 자동으로 풀린다. **배포 직후 발견된 회귀**: 판독 불가 버튼은 타이핑과 달리 체크박스를 안 껐다 — 기본이 미체크였을 때는 우연히 안 드러났지만, 기본이 체크로 바뀌면서 "판독 불가를 고르고 제출"이 매번 "판독 불가는 패스로 처리할 수 없습니다" 에러로 막히는 게 일상적으로 드러났다. **같은 날 수정** — 타이핑 시작 시 체크를 끄던 로직(`uncheckPassBox`)을 판독 불가 버튼 클릭에도 그대로 적용해, 판독 불가를 고르는 순간 체크가 자동으로 풀리게 했다 |
| admin 완료 여부 필터 + 진행중 카드 분리 (§4.5) | "완료/진행중" 필터는 서버 파라미터(`status`)만 있고 화면 컨트롤이 없었다. 진행 현황의 "진행중" 카드도 전체 합계 하나뿐이라 어느 검수자가 밀리고 있는지 알 수 없었다 | **둘 다 화면에 추가했다** (2026-08-31). ① 검수자 필터 옆에 "완료 여부" 드롭다운(전체 / 완료(2명) / 진행중(1명)) 신설 — 원래 있던 `status` 파라미터를 그대로 쓴다. ② 진행중 카드를 검수자별로 분리 — 의견이 정확히 1건인 레코드를 `reviewer_id`로 묶는 새 집계(`in_progress_by_reviewer`)를 추가했고, 현역 검수자 수만큼 카드가 자동으로 생긴다(검수자가 늘어도 코드 수정 불필요) |
| admin 목록의 "0건" 안내 (§4.5) | 결과가 0건이면 "조회된 데이터가 없습니다"만 보였다 — 필터가 8개 가까이 되면서, 어떤 필터 때문에 0건인지 알 방법이 없었다 | **적용 중인 필터 목록 + [필터 초기화] 버튼을 함께 보여준다** (2026-08-31). 검수자 화면(§4.1)에 이미 있던 동작(`activeFilterLabels`/`resetFilters`)을 admin에도 그대로 이식했다 |
